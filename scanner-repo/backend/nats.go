package backend

import "github.com/nats-io/nats.go"

func NewConnection(addr, credFile string) (nc *nats.Conn, err error) {
	// Set the error handler functions & credentials in nats.Options
	// Utilize time.Ticker to establish a nats connection
	return nil, nil
}

// here are the error handler function definitions.
